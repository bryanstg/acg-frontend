import React from "react";

export const Tabs = () => {
	return (
		<div className="tabs">
			<div className="tab">{`Pestaña ${1}`}</div>
			<div className="tab">{`Pestaña ${2}`}</div>
			<div className="tab">{`Pestaña ${3}`}</div>
			<div className="tab">{`Pestaña ${4}`}</div>
		</div>
	);
};
